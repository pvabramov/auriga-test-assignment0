The test assignment project
===========================

The project tries to implement coreutils `ls -l' command.

The program has been tested in GNU/Linux system.

To build project run following commands:

```
$ mkdir build
$ cd build
$ cmake ..
$ make
$ ./myls
```
